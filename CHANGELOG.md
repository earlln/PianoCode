# 변경 기록 (Changelog)

이 프로젝트는 [Keep a Changelog](https://keepachangelog.com/ko/1.1.0/) 형식을 따르고,
[유의적 버전(SemVer)](https://semver.org/lang/ko/)을 사용합니다.

버전은 `version.properties` 한 곳에서 관리하며, `v<버전>` 태그를 밀면
GitHub Actions가 APK를 빌드해 릴리즈에 첨부합니다.

## [Unreleased]

## [1.18.0] - 2026-09-03

손 그림을 **교재에 나오는 운지 그림처럼** 다시 그렸습니다.

### 바뀜 (Changed)

- **크림색 채움에 진한 외곽선**, 그 위에 **손톱**. 손톱이 손으로 읽히게 하는 데
  생각보다 큰 몫을 합니다. 손가락 방향에 맞춰 기울여 그립니다.
- **손바닥과 손가락을 하나의 윤곽선으로 합쳐서** 그립니다. 따로 그리면 손가락이
  손바닥에 붙는 자리마다 이음매가 보였습니다. 이제 손 전체가 테두리 하나로 둘러싸입니다.
- **손가락 번호가 손가락 위에 빨간 글씨로** 올라갑니다. 악보에 운지를 적는 방식 그대로이고,
  옅은 손가락이 이미 배경 역할을 하므로 동그라미가 필요 없습니다.
  손 모양을 끄면 예전처럼 동그라미가 다시 나옵니다 — 색이 진한 건반 위에서는
  맨 숫자가 잘 안 읽히니까요.
- 손가락을 굵게, 손바닥을 크게 키웠습니다. 이전 것은 손이라기엔 말랐습니다.
- **손 모양 진하기 기본값이 92%**입니다. 그림처럼 또렷하게 보이는 값입니다.
  건반이 비쳐 보이는 편이 좋으면 `설정 → 손 모양 진하기`를 낮추세요 (35%까지).

## [1.17.0] - 2026-09-01

손이 **거꾸로 그려지던 것**을 바로잡고, 훨씬 손답게 다시 그렸습니다.

### 고쳐짐 (Fixed)

- **손 방향이 반대였습니다.** 연주자는 건반 앞쪽에 앉습니다. 위에서 내려다본 그림에서
  앞쪽은 **아래**이므로, 손목은 그림 아래에서 들어오고 손등은 건반 앞머리에 놓이며
  손가락은 **위쪽(건반 안쪽)으로** 뻗어야 합니다. 지금까지는 정확히 뒤집혀 있었습니다.
  이제 실제로 손을 내려놓았을 때 보이는 모습과 같은 방향입니다.
- 음 이름이 손바닥에 가리던 문제. 손을 켜면 이름이 손바닥 위쪽으로 올라가고,
  손을 다 그린 뒤에 씁니다.

### 바뀜 (Changed)

- **손가락이 손가락처럼 생겼습니다.** 굵기가 일정한 막대는 아무리 색을 입혀도 철사로
  보입니다. 이제 뿌리에서 끝으로 갈수록 가늘어지고 끝이 둥글게 마무리됩니다.
  손바닥은 손가락 뿌리를 잇고 손목 쪽으로 좁아지며 화면 아래로 빠져나갑니다.
  엄지는 손 옆에서 들어오고, 손등의 마디는 가운데가 살짝 높게 아치를 그립니다.
- 색을 살구빛으로 바꿨습니다. 회보라색보다 손으로 읽힙니다.
  진하게 느껴지면 `설정 → 손 모양 진하기`를 낮추세요.

## [1.16.0] - 2026-09-01

손 모양과 밀어서 자리바꿈이 **코드 목록에서도** 되고, 표시 방식을 **설정에서 고를 수** 있습니다.

### 고쳐짐 (Fixed)

- **손 모양과 밀어서 자리바꿈이 코드 목록에서는 안 되던 문제.** 1.15.0에서 이 둘을
  코드 상세 화면에만 넣었습니다. 목록의 작은 건반에는 손가락 번호만 붙어 있었으니,
  목록에서 아무리 밀어도 자리가 바뀌지 않고 손도 나타나지 않는 것이 맞았습니다.
  이제 목록의 건반도 밀면 자리바꿈이 넘어가고, 자리를 옮기면 그 아래에 이름이 나옵니다.
  손을 켜면 그 건반도 손이 들어갈 만큼 높아집니다.

### 추가됨 (Added)

- **설정 화면.** 서랍 메뉴의 `설정`에서 건반 그림을 조절합니다.
  - `손가락 번호` — 1(엄지)~5(새끼) 표시 여부
  - `손 모양` — 실루엣 표시 여부
  - `손 모양 진하기` — 15%부터 90%까지. 옅게 두면 건반이 그대로 비쳐 보입니다
  - `코드 목록에서도 손 모양` — 목록 건반이 커지는 대신 한 화면에 보이는 코드 수가 줄어듭니다
  - `음 이름` — 건반 위 음 이름 표시 여부
  - `밀어서 자리바꿈` — 끄면 상세 화면의 칩으로만 바꿉니다
  - `기본값으로 되돌리기`
- 설정 화면 맨 위에 **살아 있는 예시 건반**이 있습니다. 스위치를 옮기는 대로 바로
  다시 그려집니다. 그림에 대한 설정을 글로만 설명하는 것보다 보여 주는 편이 빠릅니다.
- 고른 설정은 **앱을 껐다 켜도 유지**됩니다.

## [1.15.0] - 2026-09-01

건반에 **어느 손가락을 올리는지**가 표시되고, **건반을 밀어 자리바꿈**을 넘길 수 있습니다.

### 추가됨 (Added)

- **손가락 번호.** 눌러야 할 건반 위에 동그라미와 번호가 붙습니다. 1이 엄지, 5가 새끼입니다.
  불이 들어온 건반은 *무엇을* 누를지는 알려 줘도 *어떻게* 짚을지는 말해 주지 않는데,
  코드를 익히는 사람이 막히는 지점은 대개 그쪽입니다.
  - 3화음은 배우는 대로입니다. `C`는 1-3-5, 첫째 자리바꿈 `C/E`는 4도가 위에 오므로
    벌림을 중지와 새끼 사이에 두어 1-2-5가 됩니다.
  - 7화음은 약지를 건너뛴 1-2-3-5. 손이 편한 자리입니다.
  - **왼손이 짚는 자리는 어두운 동그라미**입니다. 분수코드의 베이스는 왼손 몫이고,
    한 손에 다 안 들어가는 코드(13화음 등)는 아래쪽부터 왼손으로 넘어갑니다.
- **손 모양.** 번호만으로는 어느 손가락인지는 알아도 손이 무엇을 하는지 —
  네모지게 놓이는지 뻗는지, 엄지가 어디로 들어오는지, 벌림이 얼마나 되는지 —
  는 알 수 없습니다. 이제 건반 위에 손 실루엣이 얹혀서 한눈에 보입니다.
  손등이 건반 안쪽에, 손가락이 연주자 쪽으로 뻗는 방향입니다.
  사진이 아니라 실루엣인 이유는 두 가지입니다. 밑의 건반이 계속 보여야 하고,
  남의 손 사진보다 자기 손을 겹쳐 볼 수 있는 형태가 더 쓸모 있어서입니다.
- **건반을 좌우로 밀면 자리바꿈이 넘어갑니다.** 아래 칩으로도 되지만, 이미 그림에 손을
  올려 둔 사람이 자리를 옮기려고 손을 뗄 이유는 없습니다. 길게 밀면 여러 자리를 지나갑니다.
- 코드 목록의 작은 건반에도 손가락 번호가 붙습니다. 음 이름은 그 크기에 안 들어가지만
  번호는 들어가고, 음 이름은 바로 윗줄에 이미 적혀 있습니다.

## [1.14.0] - 2026-08-31

변환된 코드를 **악보가 쓰던 표기 그대로** 씁니다.

### 바뀜 (Changed)

- **원본 표기를 따라갑니다.** `G(add2)`는 이제 `C(add2)`로 돌아옵니다. 예전에는 `Cadd9`로
  적었는데, 같은 코드이긴 해도 연주자는 이미 아는 악보를 보고 있는 터라 표기가 바뀌면
  한눈에 다른 코드로 읽힙니다. `CΔ7`은 `FΔ7`로, `C-7`은 `G-7`로 — 코드가 무엇인지는
  앱이 알아내지만, 그것을 어떻게 적을지는 악보의 몫입니다.
- 스캔이 삼킨 괄호는 **채워서** 씁니다. `G(add2`를 읽었어도 `C(add2)`라고 적습니다.
  읽은 것은 맞았고 인쇄된 괄호만 얇았을 뿐이니까요.
- **손으로 고친 코드는 앱의 표준 표기로 적습니다.** 읽어 낸 글자가 그 코드를 가리키지
  않게 된 이상, 그 표기를 흉내 낼 근거가 없습니다.
- 목록에 보이는 코드도 **실제로 그려질 표기**로 바뀝니다. 목록과 악보가 서로 다른 이름을
  말하는 일이 없습니다.

### 고쳐짐 (Fixed)

- **`G(add2)`처럼 괄호로 텐션을 적은 코드를 읽지 못하던 문제.** 코드 이름표에 `add9`와
  `(add9)`는 있었지만 `(add2)`는 없어서, 괄호까지 통째로 맞춰 보다가 실패했습니다.
  괄호는 음 이름이 이미 말하고 있는 것 외에 아무 뜻도 없으므로, 이제 맞는 이름이 없으면
  **괄호를 떼고 한 번 더** 찾아봅니다. `G(add2)` · `C(add4)` · `A7(b9)` · `Am(add9)`가
  모두 읽힙니다.
- **스캔이 괄호 한쪽만 남겼을 때도 읽습니다.** 인쇄된 괄호는 얇아서 한쪽이 자주 날아갑니다.
  `G(add2`나 `Gadd2)`도 같은 코드로 읽습니다. (`G(`처럼 괄호만 남은 것은 코드가 아닙니다.)

## [1.13.0] - 2026-08-31

`원본`은 **찍은 그대로**를 보여 주고, 앱이 읽은 내용은 **악보 잉크와 다른 색**으로 적힙니다.

### 바뀜 (Changed)

- **`원본`은 아무 표시가 없습니다.** 변환본이 있는 동안 `원본`은 사진 그대로입니다.
  테두리도, 글자도, 앱이 덧붙인 것은 하나도 없습니다. 비교란 결국 손대지 않은 쪽이
  어땠는지를 보는 일이니, 그 위에 표시가 얹혀 있으면 비교가 되지 않습니다.
  고치는 일은 `변환본` 쪽에서 하고, 전환해도 확대한 자리는 그대로 유지됩니다.
- **앱이 읽은 내용은 어두운 칩에 흰 글씨로 적힙니다.** 지금까지는 변환 표시색과 같은
  색이었는데, 변환본 위에서는 그 색이 곧 **앱이 악보에 새로 써넣은 잉크**입니다.
  같은 색으로 적으니 악보에 대한 설명이 아니라 잉크가 하나 더 있는 것처럼 보였습니다.
  이제 어느 표시색을 쓰든 한눈에 구분됩니다. 고른 것은 파란 칩, 꺼 둔 것은 회색 칩,
  아직 넣지 않은 자리는 분홍 칩입니다.

## [1.12.0] - 2026-08-31

**분수코드를 고를 수 있고**, 크게 보기와 직접 고치기가 **한 화면**이 되었습니다.

### 고쳐짐 (Fixed)

- **`A/C#` 같은 분수코드로 고칠 수 없던 문제.** 코드 고르기 화면에 근음과 계열만 있고
  베이스를 정할 자리가 없었습니다. 리드시트에 흔한 `A/C#`·`E/G#`을 표현할 방법이
  아예 없었으니, 그렇게 읽힌 자리는 고를 수 있는 정답이 없었던 셈입니다.
  이제 **`베이스 (분수코드)`** 줄에서 `없음` 또는 원하는 음을 고르면 됩니다.

### 바뀜 (Changed)

- **크게 보기와 직접 고치기가 한 화면이 되었습니다.** 악보를 키우는 일과 코드를 고치는
  일은 같은 자리에서 벌어지는데, 화면이 나뉘어 있으니 들어가기 전에 이번이 어느 쪽인지
  정해야 했습니다. 이제 하나입니다. `원본` / `변환본` 전환도 그 안에 있습니다.
- **`크게 보기` · `직접 고치기` 버튼이 없어졌습니다.** 두 손가락으로 벌리거나 두 번
  두드리면 열리므로, 같은 일을 하는 버튼을 그 옆에 둘 이유가 없습니다. 대신 그림 아래에
  어떤 동작인지 한 줄로 적어 둡니다. 한 손가락 끌기는 건드리지 않으므로 **그림 위에서
  목록 스크롤은 그대로** 됩니다.
- **코드를 꾹 누르면 바로 고칩니다.** 고를 것을 고르고 버튼을 누르는 두 단계가
  한 번으로 줄었습니다. 여러 개를 한꺼번에 지우거나 고칠 때는 예전처럼 짧게 눌러
  골라 두면 됩니다.
- **빈 곳을 짧게 누르면 선택만 풀립니다.** 예전에는 코드 고르기 창이 떴는데, 읽기만
  하려던 사람에게는 방해였습니다. 빈 곳에 코드를 새로 넣으려면 **꾹** 누르세요.

## [1.11.0] - 2026-08-31

**원본과 변환본을 확대한 자리 그대로 번갈아 볼 수 있습니다.**

### 추가됨 (Added)

- **`원본과 비교`.** 크게 보기 화면 위에 `원본` / `변환본` 단추가 생겼습니다. 누르면
  그림만 바뀌고 **배율과 위치는 그대로** 남습니다. 같은 마디가 화면 같은 자리에 있으니
  코드 한 줄을 다른 한 줄에 곧바로 겹쳐 볼 수 있습니다. 두 장을 나란히 놓는 방법도
  있지만, 그러면 그러잖아도 작은 악보가 절반이 됩니다.
- 변환본은 위에 안내 띠가 붙어 원본보다 키가 큽니다. 그만큼 끌어올려 그리므로
  **두 악보의 오선이 정확히 같은 자리**에 옵니다. 띠 높이만큼 어긋나 보이지 않습니다.
- 오른쪽 아래 배율 표시가 지금 어느 쪽을 보고 있는지도 알려 줍니다 (`300% · 변환본`).

## [1.10.0] - 2026-08-31

그냥 보기만 할 때도 악보를 확대할 수 있고, 고친 코드가 **그 자리에서 바로 코드가 됩니다.**

### 추가됨 (Added)

- **`크게 보기`.** 고칠 생각 없이 악보만 크게 읽고 싶을 때가 있습니다. 그림 아래 버튼이나
  그림을 두 번 두드리면 열립니다. 확대·이동만 되고 테두리도 선택도 없으니, 잘못 눌러서
  무언가 바뀔 일이 없습니다. 두 번 두드리면 그 자리를 300%로 당기고, 다시 두드리면 원래대로.
- **편집기가 각 자리에 앱이 읽은 내용을 적어 줍니다.** 확대하면 테두리 위에 그 자리를
  무엇으로 알고 있는지 표시됩니다. 분홍색 자리에는 읽어 낸 글자 그대로 나오므로,
  왜 코드로 인정받지 못했는지가 바로 보입니다. (`Cdm7`이면 `C#m7`을 잘못 읽은 것입니다.)

### 고쳐짐 (Fixed)

- **고친 코드가 코드로 등록되지 않던 문제.** 코드 고르기 화면은 무엇을 고치는 중인지를
  화면에 남은 선택 상태에서 되짚어 읽었습니다. 그 선택이 먼저 풀리면 고른 코드를 적을
  대상이 사라지고, 아무 일도 일어나지 않은 채 조용히 끝났습니다. 테두리는 그대로인데
  코드로는 인식되지 않은 자리가 이렇게 생겼습니다. 이제 고르기를 시작하는 순간
  **무엇을 고치는 중인지 자체를 붙들고** 있으므로, 고른 코드는 반드시 그 자리에 적힙니다.
- **`코드로 넣기`로 넣은 자리는 즉시 코드가 됩니다.** 분홍색 표시가 그 자리에서 사라지고
  변환 대상 개수에 바로 반영됩니다.

## [1.9.0] - 2026-08-31

고친 것을 **어떻게 반영하는지**가 편집기 안에서 분명해졌고, **고른 자리 옆에서 바로**
고치거나 지울 수 있습니다.

### 추가됨 (Added)

- **`고친 대로 악보에 바꿔 그리기` 버튼이 편집기 안에 생겼습니다.** 지금까지 편집기에는
  `닫기`밖에 없었습니다. 닫는 것은 반영이 아닌데 반영하는 방법이 그 화면에 없었으니,
  다 고치고 나와도 원래대로로 보이는 것이 당연했습니다. 이제 편집기에서 바로 누르면
  고친 내용으로 악보를 다시 그리고 결과 화면으로 돌아옵니다. 그냥 닫고 싶으면
  `그냥 닫기`입니다.
- **버튼이 고른 자리를 따라옵니다.** 코드를 고르면 `고치기` · `지우기` · `해제`가
  **그 코드 바로 옆에** 뜹니다. 몇 밀리미터짜리 글자를 짚으려면 확대해야 하는데,
  버튼이 화면 끝에 붙어 있으면 고칠 때마다 축소 → 손 뻗기 → 다시 확대를 반복해야 했습니다.
  화면 밖으로 나갈 자리면 위쪽에, 그것도 어려우면 보이는 곳에 붙습니다.

### 고쳐짐 (Fixed)

- **확대/축소 버튼이 화면 밑에 잘려 무슨 버튼인지 알 수 없던 문제.** 대화상자 안에서는
  시스템 여백이 잡히지 않아, 맨 아래 줄이 제스처 바 아래로 밀려 있었습니다.
  이제 그림 위 오른쪽 아래에 떠 있어 잘릴 자리가 없고, 배율과 찾은 코드 개수도 함께 보입니다.

## [1.8.3] - 2026-08-31

변환 버튼을 찾으러 스크롤할 일도, 그림 위에서 스크롤이 막힐 일도 없습니다.

### 바뀜 (Changed)

- **변환·저장·공유·직접 고치기 버튼이 그림 바로 아래로 올라왔습니다.** 지금까지는 조성
  선택과 인식된 코드 목록을 전부 지나쳐 맨 아래까지 내려가야 이 화면이 존재하는 이유인
  그 버튼을 누를 수 있었습니다. 조성과 세부 설정은 그대로 아래에 있습니다.
- **`직접 고치기`가 버튼이 되었습니다.** 그림을 두 번 두드려도 열립니다.

### 고쳐짐 (Fixed)

- **그림 위에서는 스크롤이 안 되던 문제.** 그림에 두 손가락 확대를 감지하는 처리가 붙어
  있었는데, 이것이 그림 위를 지나는 **모든 끌기 동작을 삼켰습니다.** 화면을 가득 채운
  그 그림이 곧 스크롤이 막히는 영역이었으니, 옆 여백을 찾아 눌러야만 겨우 내려갔습니다.
  확대 감지를 걷어내고 두드림만 봅니다. 두드림은 끌기를 가져가지 않으므로,
  이제 그림 위 어디를 끌어도 목록이 따라 움직입니다.

## [1.8.2] - 2026-08-31

`지우기`를 누르면 편집 화면이 뒤집히던 문제와, 버튼이 화면 맨 아래에 깔려 있던 문제를 고쳤습니다.

### 고쳐짐 (Fixed)

- **`지우기`를 누르면 편집 화면에서 튕겨 나오던 문제.** 편집기가 *변환 결과* 그림을 띄우고
  있었는데, 코드를 지우면 그 결과가 무효가 되면서 그림이 원본 사진으로 바뀌었습니다.
  배너 높이만큼 위치까지 어긋나니, 확대해 둔 자리가 사라지고 편집기에서 빠져나온 것처럼
  보였습니다. 이제 편집기는 **항상 인쇄된 원본 악보**를 보여 줍니다. 어차피 고칠 때 묻는
  것이 `악보에 원래 적힌 코드`이므로, 눈앞에 있어야 할 것도 원본입니다.
- **버튼이 화면 맨 밑에 있던 문제.** `코드 고치기` · `지우기` · `해제`가 제목 바로 아래로
  올라왔습니다. 코드를 고르는 곳은 화면 가운데인데 다음 동작은 화면 끝에 있었습니다.
  선택한 것이 없을 때도 같은 자리를 지키므로, 누를 때마다 화면이 밀리지 않습니다.
- **버튼이 제스처 바에 가려지던 문제.** 편집기가 상태 표시줄과 내비게이션 바 여백을
  직접 잡습니다. 맨 아래 버튼이 시스템 제스처 바에 깔려 눌리지 않던 문제가 사라집니다.

## [1.8.1] - 2026-08-31

동작은 1.8.0과 같습니다.

수정 내용을 악보별로 **저장해 두는 기능을 한때 넣었다가 되돌렸습니다.**
고친 내용은 의도대로 **그 이미지를 쓰는 동안만** 유지됩니다.
다른 사진을 불러오면 초기화되고, 앱 저장소에는 아무것도 남지 않습니다.

## [1.8.0] - 2026-08-31

분홍색 표시를 없앨 방법이 없던 문제를 고쳤습니다.

### 추가됨 (Added)

- **분홍색 표시를 눌러 처리합니다.** 지금까지 분홍색 테두리는 보여 주기만 할 뿐
  손댈 수가 없었습니다. 이제 누르면 두 가지 중에 고를 수 있습니다.
  `코드로 넣기`는 그 자리를 코드로 인정하고 무엇이 적혀 있었는지 묻고,
  `코드 아님`은 표시를 없앱니다. 가사나 음표를 코드처럼 읽은 자리가 여기 해당합니다.
- `코드로 넣기`로 들어가면 **읽어 낸 글자가 코드 고르기 화면에 함께 표시**됩니다.
  무엇을 보고 표시한 자리인지 알 수 있어야 무슨 코드였는지 짚어 낼 수 있습니다.
- **편집기에 범례가 생겼습니다.** 어떤 색 테두리가 무슨 뜻인지 화면 아래에 적힙니다.

### 고쳐짐 (Fixed)

- **지운 오인식이 분홍색으로 되살아나던 문제.** 코드를 지우면 그 자리를 덮고 있던
  코드가 사라지면서, 같은 자리가 이번에는 `읽었지만 넣지 못한 곳`으로 다시 표시됐습니다.
  지운다는 말이 무색했습니다. 이제 코드를 지우면 그 자리의 분홍색 표시도 함께 사라집니다.
- 분홍색 표시를 누를 때 **코드가 먼저 잡히도록** 순서를 바로잡았습니다. 코드 상자와
  분홍색 표시가 겹쳐 있으면 코드를 먼저 고르므로, 겹친 자리에서도 코드를 고칠 수 있습니다.
- `원래 조성으로 남는 부분` 개수에서 **없앤 표시는 빠집니다.** 코드가 아니라고 짚어 준
  자리까지 남은 것으로 세면, 다 처리하고도 경고가 사라지지 않습니다.

## [1.7.0] - 2026-08-30

직접 고치기가 **결과 위에 덧칠하는 방식에서, 인식 결과 자체를 고치는 방식**으로 바뀌었습니다.

### 바뀜 (Changed)

- **고치면 앱이 그렇게 기억합니다.** 지금까지는 결과 그림 위에 코드를 덧그렸을 뿐이라
  앱은 여전히 잘못 읽은 상태였습니다. 이제 고친 내용이 **인식 결과 자체**에 반영되어,
  `다시 변환`을 누르면 고친 내용까지 포함해 전체가 새로 변환됩니다.
  조옮김 대상 조성을 바꿔도 고친 코드가 그대로 따라옵니다.
- **`열기` 버튼을 없앴습니다.** 결과 그림을 **두 손가락으로 벌리면** 바로 크게 열립니다.
  확대하려는 순간이 곧 코드를 자세히 보려는 순간이니, 버튼을 먼저 찾게 하지 않습니다.

### 추가됨 (Added)

- **여러 개를 한 번에 고칩니다.** 같은 오인식이 페이지 곳곳에 반복되는 일이 흔하므로,
  코드를 여러 개 골라 한 번에 같은 코드로 바꿀 수 있습니다.
- **코드가 아닌 것을 지웁니다.** 음표나 오선이 코드로 잘못 읽힌 자리는 골라서
  `지우기`로 없앨 수 있습니다.
- 편집기가 **앱이 찾은 코드를 모두 테두리로 표시**합니다.
  선택한 것은 파란색, 꺼 둔 것은 회색, 읽었지만 쓰지 못한 자리는 분홍색입니다.

### 고쳐짐 (Fixed)

- **변환된 코드 글자 크기가 제각각이던 문제.** 인식된 상자 높이에 글자를 맞추다 보니
  상자가 글자를 얼마나 바짝 감쌌는지에 따라 크기가 들쭉날쭉했습니다.
  이제 페이지 전체가 **하나의 크기**(상자 높이의 중앙값)로 그려집니다.
  옆 코드와 겹칠 때만 조금 줄어들고, 그마저도 80% 아래로는 내려가지 않습니다.

## [1.6.0] - 2026-08-30

1.5.0의 직접 고치기가 실제로는 잘 눌리지 않았습니다. 두 가지가 문제였습니다.

### 고쳐짐 (Fixed)

- **손가락 제스처가 목록 스크롤에 먹히던 문제.** 미리보기가 설정 목록 안에 있어서
  그림 위를 긋는 동작을 목록이 스크롤로 가져갔습니다.
  이제 **전체 화면 편집기**로 분리했습니다.
- **너무 작아서 못 누르던 문제.** 폰 너비에 맞춘 악보에서 코드 심볼은 몇 밀리미터입니다.
  편집기에서 **두 손가락으로 확대·이동**할 수 있고, 확대/축소 버튼도 있습니다.
  누르는 동작도 감싸 긋기에서 **한 번 누르기**로 바꿔 훨씬 정확합니다.

### 바뀜 (Changed)

- **이제 '변경 전' 코드를 고릅니다.** 악보는 눈앞에 옛 조성으로 펼쳐져 있으니
  거기 적힌 `C♯m7`을 고르면 되고, 그게 `Bm7`이 된다는 계산은 앱이 합니다.
  목록의 모든 항목이 `C♯m7 → Bm7`처럼 변환 결과를 함께 보여 줍니다.
- 편집기에서 **읽었지만 바꾸지 못한 자리를 분홍 테두리로 표시**합니다.
  찾아다닐 필요 없이 표시된 곳을 누르면 됩니다.
  이미 손으로 넣은 코드는 표시 색 테두리로 보입니다.

## [1.5.0] - 2026-08-30

### 추가됨 (Added)

- **직접 고치기.** 자동으로 못 바꾼 코드를 손으로 채울 수 있습니다.
  변환 설정의 `직접 고치기`를 켜고, 결과 그림에서 고칠 코드 위를 손가락으로 감싸듯
  그으면 넣을 코드를 고르는 창이 열립니다.
  - 그 자리에서 글자는 읽었지만 코드로 쓰지 못한 경우, **변환된 코드가 바로 제시**되어
    한 번 더 누르면 끝납니다. (`C♯m7`을 읽었지만 못 썼다면 `Bm7 넣기` 버튼이 뜹니다)
  - 그 외에는 근음 17개 × 55종류의 전체 목록에서 고르면 됩니다.
    구성음이 함께 보여 확인하며 고를 수 있습니다.
  - 손으로 넣은 코드는 자동 변환과 똑같이 표시 색으로 그려지고, 목록에서
    `바꾸기`·`지우기`로 다시 손볼 수 있습니다.
  - 같은 자리에 자동 변환이 있으면 손으로 넣은 쪽이 우선합니다.

### 방향 (Note)

인식률을 100%로 끌어올리려 필터를 조이면 멀쩡한 코드가 함께 사라지고,
풀면 엉뚱한 글자가 들어옵니다. 이 줄다리기를 반복하는 대신,
**자동으로 대부분을 처리하고 남은 몇 개는 손으로 확실하게** 끝내는 쪽으로 갑니다.

## [1.4.1] - 2026-08-30

1.4.0에서 인식이 43개 → 39개로 더 줄었습니다. 가사 오염(`,G`)은 사라졌지만,
그걸 막으려고 넣은 통계적 필터가 멀쩡한 코드까지 걷어냈습니다.

### 고쳐짐 (Fixed)

- **행 단위 통계 필터를 걷어냈습니다.** 같은 코드 행이라도 조각마다 인식되는 세로 위치가
  조금씩 달라, 한 행이 둘로 쪼개지면 `A` `D` 같은 낱글자 행이 통째로 버려졌습니다.
  15마디의 `A`, `F♯m7`, `C♯m7`이 이렇게 사라졌습니다.
- **대신 거절 이유를 상자에 붙여 다닙니다.** 가사 사이에 혼자 있어서 거절된 판정은
  그 사실을 기억하고, 나중에 가사가 잘려 나간 조각이 같은 자리를 코드로 받아들여도
  **거절이 이깁니다.** 통계가 아니라 근거로 막으므로 멀쩡한 코드는 건드리지 않습니다.
- **크기 필터를 느슨하게 했습니다** (0.55~1.8배 → 0.45~2.4배).
  코드를 잘못 버리면 악보가 두 조성으로 갈라지지만, 잘못 남은 판정은 색으로 표시되고
  목록에서 끌 수 있습니다. 놓치는 쪽이 더 나쁩니다.

### 방향 (Note)

한 장의 악보를 보며 필터를 조이고 푸는 일을 반복하다 보니 44 → 43 → 39으로
오히려 나빠졌습니다. 이제 **재현율을 우선**하고, 잘못 들어온 판정은
색 구분과 코드 목록으로 사용자가 걸러내도록 맡깁니다.

## [1.4.0] - 2026-08-30

1.3.1에서 인식이 44개 → 43개로 오히려 하나 줄었습니다. 원인을 찾아 보니
1.3.0의 두 변경이 서로를 상쇄하고 있었습니다.

### 고쳐짐 (Fixed)

- **타일 확대가 사실상 사라졌던 문제.** 작업 해상도를 3200px로 올리면서 타일 하나가
  1800px 가까이 커졌는데, 확대 목표 폭은 2200px 그대로였습니다. 결과적으로 확대율이
  **1.64배 → 1.23배로 떨어져** 해상도를 올린 효과를 스스로 깎아먹고 있었습니다.
  - 페이지를 좌우 2등분에서 **3등분**으로 바꾸고 목표 폭을 3000px으로 올렸습니다.
    확대율이 **2.45배**로 회복됩니다.
- **가사 사이에 코드가 찍히던 문제(`,G`).** 여러 번 나눠 읽다 보면 어떤 조각에서는
  가사가 잘려 나가, 그 조각만 보면 한글이 없는 것처럼 보입니다. 한글 여부만으로는
  막을 수 없었습니다.
  - 이제 **행 단위**로 봅니다. 리드시트의 코드는 단마다 5개 이상씩 줄지어 있으므로,
    혼자 떨어져 있거나 둘뿐인 낱글자 행은 코드 행이 아니라고 보고 제외합니다.
    `E/G♯`처럼 가사에 나올 수 없는 코드가 그 행에 있으면 짧아도 살립니다.

### 추가됨 (Added)

- **인식 전처리.** 조각마다 색을 빼고 대비를 올려서, 흐린 인쇄나 사진의 회색 배경에서
  코드 심볼의 가는 획이 배경과 분리되도록 했습니다.

## [1.3.1] - 2026-08-30

1.3.0에서 같은 악보 인식이 41개 → **44개**(전체 45개)로 올라갔습니다.
대신 오인식 하나가 눈에 띄게 드러났습니다.

### 고쳐짐 (Fixed)

- **오선 위에 큰 글자가 찍히던 문제.** 음표나 오선 일부가 글자로 잘못 읽히면 그것도
  코드로 변환되어 악보 위에 그려졌습니다. 게다가 글자 크기를 인식된 상자 높이에
  그대로 맞추기 때문에, 상자가 큰 오인식은 **음악을 가릴 만큼 큰 글자**가 됐습니다.
  - 인쇄된 악보의 코드 심볼은 모두 같은 크기입니다. 이제 찾아낸 코드들의 **중앙값 높이**를
    기준으로, 거기서 크게 벗어나는 것은 코드가 아니라고 보고 제외합니다.
    제외된 것은 `원래 조성으로 남는 부분` 개수에 포함되어 알려집니다.
  - 그려질 때도 중앙값 기준으로 크기 상한을 둡니다. 잘못된 상자가 걸러지지 않고
    통과하더라도 악보를 덮어쓸 만큼 커지지 않습니다.

## [1.3.0] - 2026-08-30

1.2.0에서 같은 악보의 인식이 45개 중 32개 → **41개**로 올라갔습니다.
남은 4개를 좁히기 위해 인식 해상도를 더 확보했습니다.

### 고쳐짐 (Fixed)

- **작업 해상도를 2400px → 3200px로 올렸습니다.** 원본을 줄인 뒤 다시 확대하면
  없는 화소를 만들어낼 뿐이라, 애초에 버리지 않도록 바꿨습니다.
- **가로 띠를 좌우로 한 번 더 나눠 읽습니다.** 페이지 폭 전체를 한 조각으로 읽으면
  확대해도 글자 하나에 돌아가는 화소가 부족합니다. 절반 폭으로 자르면 같은 심볼이
  두 배 크기로 읽힙니다. 조각은 가로·세로 모두 겹쳐 잘라, 경계에 걸친 코드가
  옆 조각에서 온전히 잡힙니다.

### 추가됨 (Added)

- **바뀐 코드를 색으로 구분합니다.** 새로 쓴 코드에만 색을 입히고, 원래 조성으로 남은
  코드는 악보 원래 색 그대로 둡니다. 지금까지는 원본 잉크색을 그대로 써서 무엇이
  바뀌었는지 눈으로 알 수 없었고, 절반만 변환된 악보도 완성된 것처럼 보였습니다.
  인쇄용으로 깔끔하게 뽑고 싶으면 스위치로 끌 수 있습니다.
- **표시 색은 악보에 맞춰 자동으로 고릅니다.** 보라색으로 인쇄되거나 필기된 악보라면
  보라색 표시가 묻히기 때문에, 그 악보의 잉크색을 재서 **가장 다른 색**을 씁니다.
  보라·빨강·파랑·초록·주황 중에서 직접 고를 수도 있습니다.
  다섯 색 모두 흰 종이·미색 종이에서 읽히고 복사에도 견디는 진하기입니다.
- **해상도 경고.** 이미지 가로가 1600px 미만이면 코드 심볼이 작아 인식률이 떨어진다고
  미리 알려 줍니다. 화면 캡처는 원본 파일이나 가까이 찍은 사진보다 불리합니다.
- 인식에 몇 초 걸린다는 안내를 진행 표시에 넣었습니다.

### 참고 (Notes)

- 조각을 나눠 여러 번 읽으므로 **변환 시간이 늘어납니다.** 정확도와 맞바꾼 부분입니다.
- 큰 페이지를 다루기 위해 `largeHeap`을 켰습니다.

## [1.2.0] - 2026-08-30

실제 악보(손경민 `충만`, A → G)를 변환해 보니 코드 45개 중 **32개만 인식**되었고,
나머지는 원래 조성 그대로 남아 한 악보에 두 조성이 섞였습니다. 원인은 변환이 아니라
**인식률**이었습니다.

### 고쳐짐 (Fixed)

- **인식률.** 악보 한 장을 통째로 넘기면 코드 심볼 높이가 20픽셀 안팎이라 글자 인식의
  한계선에 걸립니다. 이제 페이지 전체를 한 번 읽고, **가로 띠로 나눠 확대해서 다시**
  읽은 뒤 두 결과를 합칩니다. 띠는 서로 겹치게 잘라, 경계에 걸친 코드가 사이로 빠지지
  않습니다. 같은 자리에서 두 번 읽힌 경우 더 온전한 쪽을 씁니다
  (`C`보다 `C♯m7`, 파싱되는 쪽 우선).
- **가사 한가운데 코드가 찍히던 문제.** 한글이 섞인 줄에서는 이제 코드가 이웃 코드와
  나란히 있을 때만 인정합니다. 음절 하나가 코드로 잘못 읽혀도 가사를 덮어쓰지 않습니다.
- **`A → G`가 `+10반음`으로 표시되던 문제.** 같은 결과지만 연주자는 `-2반음`으로 읽습니다.
  이제 짧은 쪽 방향으로 표시합니다 (`-6`~`+6`).

### 남은 한계 (Known limits)

- 인쇄 상태나 촬영 각도에 따라 여전히 놓치는 코드가 있을 수 있습니다.
  놓친 코드는 `원래 조성으로 남는 부분` 경고에 개수와 함께 표시되니,
  저장 전에 확인해 주세요.
- 오선보의 조표와 음표는 바뀌지 않습니다. 코드 심볼만 옮겨 적습니다.

## [1.1.1] - 2026-08-30

### 고쳐짐 (Fixed)

- **한 글자짜리 코드(`A`, `D`)가 변환되지 않던 문제.** 가사 줄의 낱글자를 코드로
  오인하지 않으려는 방어가, 글자 인식이 코드 줄과 바로 아래 가사 줄을 하나로 묶었을 때
  역효과를 냈습니다. 병합된 줄은 단어 수가 가사 쪽으로 기울어 "가사 줄"로 판정되고,
  그 줄의 한 글자 코드만 조용히 버려졌습니다. `E/G♯` `F♯m7` `C♯m7`처럼 두 글자 이상인
  코드는 그대로 변환되었기 때문에, 한 악보에 두 조성이 섞이는 결과가 나왔습니다.
  이제는 줄 전체 비율이 아니라 **연달아 코드로 읽히는 구간**을 보고 판단합니다.
  `E/G♯`처럼 가사에 나올 리 없는 코드가 그 구간에 있거나 구간이 세 단어 이상이면
  한 글자 코드도 함께 살립니다.
- 판정에서 제외된 코드도 이제는 `원래 조성으로 남는 부분` 경고에 잡힙니다.
  이전에는 이 경로만 아무 흔적 없이 버려졌습니다.
- 글자 인식이 남기는 괄호·중괄호·물결표 등을 코드 심볼 양끝에서 더 넓게 걷어냅니다.

### 바뀜 (Changed)

- 코드 판별 로직을 `core-music`의 `SheetTextFilter`로 옮겨 순수 Kotlin으로 만들었습니다.
  안드로이드 없이 단위 테스트할 수 있어, 실제 악보(손경민 `충만`)의 코드 줄이
  가사와 병합된 상황을 그대로 재현해 검증합니다.

## [1.1.0] - 2026-08-30

### 추가됨 (Added)

- 모든 화면 상단에 `PianoCode v<버전> by Earlln.com` 표기.
- 변환 결과 이미지 위쪽에 무엇이 바뀌었는지 적은 띠를 그립니다.
  (`PianoCode · A → C (+3반음) · 코드 심볼만 변경`)
- 변환 전에 **원래 조성으로 남는 부분**을 알려 줍니다. 코드처럼 보이지만 읽지 못한 글자와
  직접 꺼 둔 코드의 개수를 세어, 한 악보에 두 조성이 섞이는 일을 막습니다.
- 꺼 둔 코드를 한 번에 되돌리는 `꺼 둔 코드 모두 켜기` 버튼.
- **악보 이미지 가져오는 경로를 네 가지로** 늘렸습니다. 기본 사진 선택기는 한 덩어리 격자만
  보여 줘서, 악보를 앨범으로 정리해 둔 경우 찾기 어려웠습니다.
  - `최근 사진` — 안드로이드 기본 사진 선택기 (권한 불필요)
  - `갤러리 앱에서 찾기` — 삼성 갤러리·구글 포토의 사진 / 앨범 / 스토리 탐색
  - `파일에서 찾기` — 내 파일, 드라이브, 다운로드 폴더
  - `카메라로 촬영` — 악보를 지금 찍어서 바로 변환

### 고쳐짐 (Fixed)

- 변환된 코드가 잘려 그려질 수 있던 문제. 이전에는 코드를 하나씩 지우고 그렸기 때문에,
  바로 오른쪽 코드의 지우기 영역이 앞 코드의 긴 심볼(`C♯m7` → `Em7` 등) 꼬리를
  덮어 버릴 수 있었습니다. 이제 모든 자리를 먼저 지운 뒤 글자를 그립니다.
- 색 추출을 원본 이미지에서만 하도록 바꿔, 앞서 그린 글자가 다음 코드의
  종이색·잉크색 판단에 섞이지 않게 했습니다.

### 검증 (Verified)

- 실제 악보(손경민 `충만`)의 A장조 코드 45개를 C장조로 옮기는 테스트를 추가했습니다.
  `A→C`, `E/G♯→G/B`, `F♯m7→Am7`, `C♯m7→Em7`, `D→F`, `A/C♯→C/E`, `Bm7→Dm7`,
  `E7→G7`, `C♯7→E7` 전부를 마디 순서대로 확인하고, 모든 조성으로의 이조와
  A장조로의 왕복도 함께 검사합니다.

## [1.0.0] - 2026-08-30

첫 번째 정식 릴리즈입니다.

### 추가됨 (Added)

- **코드 목록**: 17개 근음 위에 55종류의 코드, 모두 935개를 담은 코드 사전.
  메이저·마이너·도미넌트·디미니쉬·오그멘티드·서스펜디드·파워 7개 계열로 나뉘며,
  각 계열마다 기본 코드와 그로부터 파생된 모든 변화 코드를 함께 볼 수 있습니다.
- **건반 그림**: 모든 코드의 눌러야 할 건반 위치를 그림으로 표시합니다.
  근음·구성음·텐션·베이스를 색으로 구분하고, 자리바꿈(전위)마다 손 모양이 바뀝니다.
- **코드 상세**: 구성음과 도수, 근음에서의 반음 거리, 코드가 어울리는 조성,
  같은 계열의 다른 코드, 다른 근음의 같은 코드를 한 화면에서 확인할 수 있습니다.
- **코드 검색**: 코드 심볼(`F#m7b5`), 한글 이름(`마이너`), 영문 이름(`diminished`)으로 검색.
- **스케일 & 다이아토닉**: 12개 스케일의 구성음을 건반에서 확인하고,
  해당 조성의 다이아토닉 3화음·7화음을 로마숫자와 함께 봅니다.
- **악보 코드 변환**: 악보 사진을 고르면 기기 안에서 글자를 인식해 코드 심볼을 찾아내고,
  원하는 조성/스케일로 바꿔 원본 이미지 위에 다시 그려 줍니다.
  - 원래 조성 자동 추정 (직접 변경 가능)
  - `조옮김` — 모든 코드를 같은 간격으로 이동
  - `스케일 맞춤` — 각 코드의 도수를 유지한 채 목표 스케일의 화성으로 변환
  - 인식된 코드를 하나씩 켜고 끄는 검토 목록, 변환 전/후 비교
  - 갤러리 저장 및 공유
- **정확한 음이름 표기**: D♭ 장조에서는 `D♭`, A 장조에서는 `C♯`처럼
  조성에 맞는 표기를 사용합니다. 음이 아니라 악보에 적히는 글자를 기준으로 계산합니다.

### 참고 (Notes)

- 글자 인식은 ML Kit의 기기 내 처리 모델을 사용합니다. 사진은 서버로 전송되지 않습니다.
- 최소 지원 버전은 Android 7.0(API 24), 타깃은 Android 15(API 35)입니다.

[Unreleased]: https://github.com/earlln/PianoCode/compare/v1.7.0...HEAD
[1.7.0]: https://github.com/earlln/PianoCode/releases/tag/v1.7.0
[1.6.0]: https://github.com/earlln/PianoCode/releases/tag/v1.6.0
[1.5.0]: https://github.com/earlln/PianoCode/releases/tag/v1.5.0
[1.4.1]: https://github.com/earlln/PianoCode/releases/tag/v1.4.1
[1.4.0]: https://github.com/earlln/PianoCode/releases/tag/v1.4.0
[1.3.1]: https://github.com/earlln/PianoCode/releases/tag/v1.3.1
[1.3.0]: https://github.com/earlln/PianoCode/releases/tag/v1.3.0
[1.2.0]: https://github.com/earlln/PianoCode/releases/tag/v1.2.0
[1.1.1]: https://github.com/earlln/PianoCode/releases/tag/v1.1.1
[1.1.0]: https://github.com/earlln/PianoCode/releases/tag/v1.1.0
[1.0.0]: https://github.com/earlln/PianoCode/releases/tag/v1.0.0
