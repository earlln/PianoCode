# 변경 기록 (Changelog)

이 프로젝트는 [Keep a Changelog](https://keepachangelog.com/ko/1.1.0/) 형식을 따르고,
[유의적 버전(SemVer)](https://semver.org/lang/ko/)을 사용합니다.

버전은 `version.properties` 한 곳에서 관리하며, `v<버전>` 태그를 밀면
GitHub Actions가 APK를 빌드해 릴리즈에 첨부합니다.

## [Unreleased]

## [1.1.0] - 2026-08-30

### 추가됨 (Added)

- 모든 화면 상단에 `PianoCode v<버전> by Earlln.com` 표기.
- 변환 결과 이미지 위쪽에 무엇이 바뀌었는지 적은 띠를 그립니다.
  (`PianoCode · A → C (+3반음) · 코드 심볼만 변경`)
- 변환 전에 **원래 조성으로 남는 부분**을 알려 줍니다. 코드처럼 보이지만 읽지 못한 글자와
  직접 꺼 둔 코드의 개수를 세어, 한 악보에 두 조성이 섞이는 일을 막습니다.
- 꺼 둔 코드를 한 번에 되돌리는 `꺼 둔 코드 모두 켜기` 버튼.
- **악보 이미지 가져오는 경로를 네 가지로** 늘렸습니다. 기본 사진 선택기는 한 덩어리 격자만
  보여 줘서, 악보를 앨범으로 정리해 둔 경우 찾기 어려웠습니다.
  - `최근 사진` — 안드로이드 기본 사진 선택기 (권한 불필요)
  - `갤러리 앱에서 찾기` — 삼성 갤러리·구글 포토의 사진 / 앨범 / 스토리 탐색
  - `파일에서 찾기` — 내 파일, 드라이브, 다운로드 폴더
  - `카메라로 촬영` — 악보를 지금 찍어서 바로 변환

### 고쳐짐 (Fixed)

- 변환된 코드가 잘려 그려질 수 있던 문제. 이전에는 코드를 하나씩 지우고 그렸기 때문에,
  바로 오른쪽 코드의 지우기 영역이 앞 코드의 긴 심볼(`C♯m7` → `Em7` 등) 꼬리를
  덮어 버릴 수 있었습니다. 이제 모든 자리를 먼저 지운 뒤 글자를 그립니다.
- 색 추출을 원본 이미지에서만 하도록 바꿔, 앞서 그린 글자가 다음 코드의
  종이색·잉크색 판단에 섞이지 않게 했습니다.

### 검증 (Verified)

- 실제 악보(손경민 `충만`)의 A장조 코드 45개를 C장조로 옮기는 테스트를 추가했습니다.
  `A→C`, `E/G♯→G/B`, `F♯m7→Am7`, `C♯m7→Em7`, `D→F`, `A/C♯→C/E`, `Bm7→Dm7`,
  `E7→G7`, `C♯7→E7` 전부를 마디 순서대로 확인하고, 모든 조성으로의 이조와
  A장조로의 왕복도 함께 검사합니다.

## [1.0.0] - 2026-08-30

첫 번째 정식 릴리즈입니다.

### 추가됨 (Added)

- **코드 목록**: 17개 근음 위에 55종류의 코드, 모두 935개를 담은 코드 사전.
  메이저·마이너·도미넌트·디미니쉬·오그멘티드·서스펜디드·파워 7개 계열로 나뉘며,
  각 계열마다 기본 코드와 그로부터 파생된 모든 변화 코드를 함께 볼 수 있습니다.
- **건반 그림**: 모든 코드의 눌러야 할 건반 위치를 그림으로 표시합니다.
  근음·구성음·텐션·베이스를 색으로 구분하고, 자리바꿈(전위)마다 손 모양이 바뀝니다.
- **코드 상세**: 구성음과 도수, 근음에서의 반음 거리, 코드가 어울리는 조성,
  같은 계열의 다른 코드, 다른 근음의 같은 코드를 한 화면에서 확인할 수 있습니다.
- **코드 검색**: 코드 심볼(`F#m7b5`), 한글 이름(`마이너`), 영문 이름(`diminished`)으로 검색.
- **스케일 & 다이아토닉**: 12개 스케일의 구성음을 건반에서 확인하고,
  해당 조성의 다이아토닉 3화음·7화음을 로마숫자와 함께 봅니다.
- **악보 코드 변환**: 악보 사진을 고르면 기기 안에서 글자를 인식해 코드 심볼을 찾아내고,
  원하는 조성/스케일로 바꿔 원본 이미지 위에 다시 그려 줍니다.
  - 원래 조성 자동 추정 (직접 변경 가능)
  - `조옮김` — 모든 코드를 같은 간격으로 이동
  - `스케일 맞춤` — 각 코드의 도수를 유지한 채 목표 스케일의 화성으로 변환
  - 인식된 코드를 하나씩 켜고 끄는 검토 목록, 변환 전/후 비교
  - 갤러리 저장 및 공유
- **정확한 음이름 표기**: D♭ 장조에서는 `D♭`, A 장조에서는 `C♯`처럼
  조성에 맞는 표기를 사용합니다. 음이 아니라 악보에 적히는 글자를 기준으로 계산합니다.

### 참고 (Notes)

- 글자 인식은 ML Kit의 기기 내 처리 모델을 사용합니다. 사진은 서버로 전송되지 않습니다.
- 최소 지원 버전은 Android 7.0(API 24), 타깃은 Android 15(API 35)입니다.

[Unreleased]: https://github.com/earlln/PianoCode/compare/v1.1.0...HEAD
[1.1.0]: https://github.com/earlln/PianoCode/releases/tag/v1.1.0
[1.0.0]: https://github.com/earlln/PianoCode/releases/tag/v1.0.0
